services:
  - type: web
    name: fatora
    env: node
    plan: free
    buildCommand: pnpm install && pnpm -r build
    startCommand: pnpm --filter @workspace/api-server start
    envVars:
      - key: PORT
        value: 10000
